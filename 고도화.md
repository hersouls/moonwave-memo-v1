전체 코드를 면밀히 분석 완료했습니다. 아래 점검 결과를 정리합니다.

---

# Tiptap 적용 상태 점검 보고서

## 전체 요약

| 등급 | 건수 | 설명 |
|------|------|------|
| 🔴 **심각** | 6건 | Tiptap 모드에서 기능이 작동하지 않음 |
| 🟡 **중요** | 5건 | 기능 누락/불완전 통합 |
| 🟢 **개선** | 4건 | 최적화/정리 기회 |
| ✅ **정상** | 8건 | 올바르게 동작 |

---

## ✅ 정상 동작 확인 (8건)

| # | 항목 | 확인 위치 |
|---|------|-----------|
| 1 | 마크다운 직렬화/역직렬화 | `useTiptapEditor.ts:38-47` — `tiptap-markdown` 설정 정상 |
| 2 | Auto-save 연결 | `MemoEditor.tsx:369-373` — `handleTiptapUpdate` → `handleBodyChange` → `scheduleAutoSave` |
| 3 | 에디터 모드 전환 | `EditorHeader.tsx:164-167` — tabs → split → tiptap 순환 |
| 4 | Undo/Redo 버튼 위임 | `MemoEditor.tsx:782-785` — Tiptap일 때 `editor.commands.undo/redo` 호출 |
| 5 | 글자 수 계산 | `MemoEditor.tsx:609` — `isTiptap ? tiptapCharCount : body.length` |
| 6 | 외부 콘텐츠 동기화 | `useTiptapEditor.ts:60-68` — 버전 복원/AI 편집 시 `setContent` |
| 7 | 다크 모드 CSS | `tiptap-editor.css:23-25` + `markdown.css` 전체 dark override |
| 8 | Placeholder | `useTiptapEditor.ts:36` — Placeholder 확장 정상 구성 |

---

## 🔴 심각한 문제 (6건)

### 🔴-1. 툴바 포맷 버튼이 Tiptap 모드에서 무동작

**위치**: `EditorToolbar.tsx:89-111, 152-158`

```typescript
// 현재: textarea DOM을 직접 조작
const insertAtCursor = (before: string, after: string = '') => {
  const textarea = textareaRef.current
  if (!textarea) return  // ← Tiptap 모드에서는 textarea가 없으므로 여기서 return
  // ... textarea 문자열 조작
}

// Bold 버튼 예시
{ action: () => insertAtCursor('**', '**') }  // ← Tiptap에서 아무 일도 안 일어남
```

**영향**: **Bold, Italic, Code, Link, List, Heading 버튼 6개 모두** Tiptap 모드에서 작동하지 않음

**수정 방향**:
```typescript
// Tiptap editor 인스턴스를 prop으로 받아서 분기
interface EditorToolbarProps {
  tiptapEditor?: Editor | null  // 추가
  // ...
}

// Tiptap일 때
{ action: () => tiptapEditor
    ? tiptapEditor.chain().focus().toggleBold().run()
    : insertAtCursor('**', '**')
}
```

---

### 🔴-2. 서식 감지(활성 상태 표시)가 Tiptap에서 불가

**위치**: `EditorToolbar.tsx:58-74`

```typescript
// 현재: textarea 선택 영역에서 regex로 감지
const detectFormats = useCallback(() => {
  const textarea = textareaRef.current
  if (!textarea) return  // ← Tiptap 모드에서 항상 return
  // ... regex 분석
})
```

**영향**: Tiptap에서 굵게/기울임 텍스트 위에 커서를 놓아도 버튼이 활성 표시되지 않음

**수정 방향**:
```typescript
// Tiptap의 내장 API 사용
if (tiptapEditor) {
  if (tiptapEditor.isActive('bold')) formats.add('bold')
  if (tiptapEditor.isActive('italic')) formats.add('italic')
  if (tiptapEditor.isActive('code')) formats.add('code')
  if (tiptapEditor.isActive('link')) formats.add('link')
}
```

---

### 🔴-3. 이미지 삽입이 Tiptap 모드에서 불가

**위치**: `EditorToolbar.tsx:209-219`

```typescript
<ImageInsertButton
  memoId={memoId}
  onInsert={(markdown) => {
    const textarea = textareaRef.current
    if (!textarea) return  // ← Tiptap 모드에서 return
    // ... textarea 문자열 조작
  }}
/>
```

**영향**: 이미지 삽입 버튼 클릭해도 아무 일도 안 일어남

**수정 방향**:
```typescript
onInsert={(markdown) => {
  if (tiptapEditor) {
    tiptapEditor.chain().focus().insertContent(markdown).run()
  } else {
    // 기존 textarea 로직
  }
}}
```

---

### 🔴-4. Undo 스택 이중 관리 (메모리 낭비 + 충돌 가능)

**위치**: `MemoEditor.tsx:220-222, 348-363, 369-373`

```
호출 흐름 (Tiptap 모드):
사용자 타이핑
  → tiptap onUpdate 발생
  → handleTiptapUpdate(markdown)
    → handleBodyChange(markdown)        
      → pushUndoSnapshot(value)  ← 수동 스택에 PUSH (불필요!)
      → setBody(value)
  → Tiptap History 확장도 자체 스택에 기록  ← 이중 기록!
```

**영향**:
- 매 입력마다 전체 body 문자열이 수동 스택에 저장 (최대 50개 × 메모 전문)
- Tiptap의 트랜잭션 기반 히스토리와 수동 스택이 별도로 존재 → 불일치 가능

**수정 방향**:
```typescript
const handleBodyChange = useCallback((value: string) => {
  if (!isTiptap) pushUndoSnapshot(value)  // Tiptap일 때는 스킵
  setBody(value)
  // ...
}, [isTiptap, ...])
```

---

### 🔴-5. 목차(Outline) 클릭 시 Tiptap에서 스크롤 불가

**위치**: `MemoEditor.tsx:143-156`

```typescript
const handleOutlineScrollToLine = useCallback((line: number) => {
  const textarea = bodyRef.current
  if (!textarea) return  // ← Tiptap 모드에서 return
  // ... textarea.scrollTop 조작
}, [])
```

**영향**: 목차 패널에서 헤딩 클릭해도 에디터가 스크롤되지 않음

**수정 방향**:
```typescript
const handleOutlineScrollToLine = useCallback((line: number) => {
  if (isTiptap && tiptapEditor) {
    // Tiptap 문서에서 n번째 heading 노드 찾아 scrollIntoView
    let headingCount = 0
    tiptapEditor.state.doc.descendants((node, pos) => {
      if (node.type.name === 'heading') {
        if (headingCount === targetIndex) {
          const dom = tiptapEditor.view.nodeDOM(pos)
          ;(dom as HTMLElement)?.scrollIntoView({ behavior: 'smooth', block: 'center' })
          return false
        }
        headingCount++
      }
    })
  } else {
    // 기존 textarea 로직
  }
}, [isTiptap, tiptapEditor])
```

---

### 🔴-6. AI Autocomplete (ghost text)가 Tiptap에서 미작동

**위치**: `MemoEditor.tsx:160, 501-517, 689-693`

```typescript
// bodyRef(textarea)를 직접 전달 — Tiptap에서는 textarea 없음
const { ghostText, acceptSuggestion, dismissSuggestion } = useAIAutocomplete(body, bodyRef)

// Tab 키 처리도 textarea keyDown 이벤트에서만
const handleBodyKeyDown = (e) => { /* Tab → acceptSuggestion */ }
// → Tiptap은 이 핸들러를 사용하지 않음

// ghost text 표시도 textarea 컨테이너 안에만
{ghostText && ( /* ... */ )}
// → Tiptap 블록에는 없음
```

**영향**: Tiptap 모드에서 AI 자동완성이 전혀 표시/동작하지 않음

---

## 🟡 중요 문제 (5건)

### 🟡-1. Tiptap 코드 블록에 구문 강조 없음

**현재**: `StarterKit`의 기본 `CodeBlock` 사용 → 하이라이팅 없음
**비교**: 미리보기 모드에서는 `rehype-highlight`로 syntax highlighting 제공

**수정 방향**: `@tiptap/extension-code-block-lowlight` + `lowlight` 라이브러리 추가

---

### 🟡-2. Tiptap 모드에서 슬래시 커맨드 미지원

**위치**: `MemoEditor.tsx:979`
```typescript
{/* Slash command menu (legacy mode only) */}
{!isTiptap && slashOpen && ( <SlashCommandMenu ... /> )}
```

Tiptap의 `@tiptap/suggestion` 확장으로 동일 기능 구현 가능하나 현재 미적용

---

### 🟡-3. Tiptap 모드에서 Floating Toolbar (BubbleMenu) 미지원

**위치**: `MemoEditor.tsx:974`
```typescript
{/* Floating toolbar for text selection (legacy mode only) */}
{!isTiptap && ...}
```

Tiptap의 `@tiptap/react`에 포함된 `BubbleMenu` 컴포넌트로 대체 가능하나 미적용

---

### 🟡-4. Tiptap TaskList CSS 불일치

**현재 CSS** (`markdown.css:140-181`):
```css
/* react-markdown이 생성하는 클래스 */
.memo-markdown-preview ul.contains-task-list { ... }
.memo-markdown-preview li.task-list-item { ... }
.memo-markdown-preview li.task-list-item input[type="checkbox"] { ... }
```

**Tiptap이 생성하는 DOM**:
```html
<!-- Tiptap은 다른 구조를 사용 -->
<ul data-type="taskList">
  <li data-type="taskItem" data-checked="true">
    <label><input type="checkbox" checked></label>
    <div>내용</div>
  </li>
</ul>
```

**영향**: Tiptap의 체크리스트가 기대한 스타일로 표시되지 않을 수 있음

---

### 🟡-5. Ambient Soundscape가 Tiptap 모드에서 미작동

**위치**: `MemoEditor.tsx:473-478`

```typescript
// 이 코드는 textarea의 handleBodyInput 안에만 있음
if (value.length > 0) {
  const lastChar = value.charCodeAt(value.length - 1)
  playAmbientNote(lastChar)  // ← Tiptap 모드에서는 호출되지 않음
}
```

`handleTiptapUpdate`에는 `playKeySound()`와 `recordKeystroke()`만 있고 `playAmbientNote()`는 빠져 있음

---

## 🟢 개선 기회 (4건)

### 🟢-1. Tiptap 에디터 항상 초기화 (불필요한 리소스)

**위치**: `MemoEditor.tsx:375-379`

```typescript
// editorMode가 'tabs' 또는 'split'이어도 항상 Tiptap 인스턴스 생성
const { editor: tiptapEditor, characterCount: tiptapCharCount } = useTiptapEditor({
  initialContent: body,
  // ...
})
```

**영향**: 모든 모드에서 ProseMirror 인스턴스 + 마크다운 파서 메모리 사용

**수정 방향**: 조건부 생성 또는 lazy 초기화 (단, React hooks 규칙 주의 필요 — 별도 컴포넌트 분리가 가장 안전)

---

### 🟢-2. 번들 이중 적재

현재 에디터 3모드를 지원하므로 **두 스택이 모두 번들에 포함**:

```
react-markdown + remark + rehype  ~53KB
@tiptap/* + tiptap-markdown       ~100KB
────────────────────────────────────────
총                                ~153KB (gzip)
```

단일 모드로 전환 시 약 35% 절감 가능

---

### 🟢-3. `isExternalUpdateRef` 동기 플래그의 위험성

**위치**: `useTiptapEditor.ts:65-68`

```typescript
isExternalUpdateRef.current = true
editor.commands.setContent(initialContent)  // 동기 실행이지만...
lastEmittedRef.current = initialContent
isExternalUpdateRef.current = false         // 즉시 해제
```

`setContent`가 내부적으로 비동기 트랜잭션을 발행할 경우 플래그 누락 가능 (현재 ProseMirror에서는 동기이므로 당장 문제는 아님, 업그레이드 시 주의)

---

### 🟢-4. Footer 조건문의 잠재적 오류

**위치**: `MemoEditor.tsx:940`

```typescript
} : (activeTab === 'edit' || isSplit) && charCount > 0 && !isKeyboardOpen && (
```

Tiptap 모드에서 `activeTab`이 우연히 `'edit'` (초기값)이라 동작하지만, 명시적으로 `isTiptap`을 포함해야 안전:
```typescript
} : (isTiptap || activeTab === 'edit' || isSplit) && charCount > 0 && ...
```

---

## 우선순위별 수정 로드맵

```
Phase 1 — 즉시 수정 (🔴 심각, 기능 불능)
─────────────────────────────────────────
1. EditorToolbar에 tiptapEditor prop 전달 + 분기 처리
   → 포맷 버튼 6개 + 이미지 삽입 + 서식 감지 한번에 해결
   (🔴-1, 🔴-2, 🔴-3)

2. handleBodyChange에서 Tiptap일 때 pushUndoSnapshot 스킵
   (🔴-4)

3. handleOutlineScrollToLine에 Tiptap 분기 추가
   (🔴-5)

Phase 2 — 기능 완성 (🟡 중요)
─────────────────────────────────────────
4. Tiptap TaskList CSS 추가
   (🟡-4)

5. CodeBlockLowlight 확장 추가
   (🟡-1)

6. Ambient Soundscape 호출 추가
   (🟡-5)

Phase 3 — Tiptap 전용 기능 (🟡 UX 향상)
─────────────────────────────────────────
7. BubbleMenu 추가 (텍스트 선택 시 플로팅 툴바)
   (🟡-3)

8. Suggestion 확장으로 슬래시 커맨드
   (🟡-2)

9. AI Autocomplete Tiptap 통합
   (🔴-6)

Phase 4 — 최적화 (🟢)
─────────────────────────────────────────
10. 에디터 인스턴스 조건부 생성
11. Footer 조건문 명시화
12. 번들 최적화 검토
```
